# {{PROJECT_NAME}}

A Commander X16 project built with ACME assembler.

Created: {{DATE}}

## Building

```
make          # Build the .prg
make run      # Build and run in emulator
make clean    # Remove build artifacts
```

## Project Structure

```
src/          Assembly source code (.a files)
include/      Include files (.a equates, macros)
assets/       Graphics, sounds, data files
build/        Build output (gitignored)
```
